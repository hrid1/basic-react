/* eslint-disable no-unused-vars */

import React from 'react';

const Countries = () => {
    return (
        <div>

            <h1>Here are the countries.</h1>
            
        </div>
    );
};

export default Countries;
